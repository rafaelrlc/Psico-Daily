import styled from "styled-components";

const TestDiv = styled.div`
  display: grid;
  place-items: center;
  height: 100vh;
`;
const test = () => {
  return <TestDiv>OI</TestDiv>;
};

export default test;
